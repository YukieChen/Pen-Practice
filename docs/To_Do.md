- `C:\GitRepo\Pen-Practice\.agent\engineering\workflows\production_flow.mdc` 流程需修正，在步驟 4，寫好工作項目計劃後就要進行第二次的 red-team，查核工作計劃是否與 specs 對齊。而不是等到程式碼都完成了才進行第二輪 red-team，那時已經太晚了。
- 每一個階段的產出都要存檔。必須規定每個階段的存檔要存在哪裡？以什麼樣的名稱為前綴？這樣子光是看檔案就可以知道進行到哪個步驟，或是某個步驟出了錯 (沒有檔案產出)？
- 程式碼存檔格式也要進行規劃。每個專案存檔方式都不一樣，每次開新專案就需要重新瞭解存檔方式，非常浪費時間。所有專案要怎麼樣建立資料夾，哪些檔案要存放哪裡？這些都要統一規定，讓所有專案一致。
- 現在 Cursor & Antigravity 都可以上網，所以第一個步驟應該調整為 AI 上網搜尋資料，與使用者討論 specs 的需求調整
